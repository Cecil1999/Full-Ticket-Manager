import { useState } from 'react';

export default function UserForm() {
  const [isFormRequestDone, setIsFormRequestDone] = useState<boolean>(true);

  const handleFormSubmission = () => {
  }

  return <>
  </>
}
